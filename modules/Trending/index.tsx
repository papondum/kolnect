import { styled } from "@/stitches.config";

interface IProps {
  //   dealer: IDealer;
  //   onClose: () => void;
  //   panel: IPanel;
}
const Wrap = styled("div", {
  //
});
const View = ({}: IProps) => {
  return <Wrap>Trending</Wrap>;
};

export default View;
