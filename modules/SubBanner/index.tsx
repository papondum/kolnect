import { styled } from "@/stitches.config";

interface IProps {
  //   dealer: IDealer;
  //   onClose: () => void;
  //   panel: IPanel;
}
const Wrap = styled("div", {
  //
});
const View = ({}: IProps) => {
  return <Wrap>Sub</Wrap>;
};

export default View;
